# Configuración del adaptador de Votación
VotacionAdapter.Endpoints=tcp -h localhost -p 10002

# Propiedades del Proxy para conectar con otros servicios
Portal.Proxy=portal:tcp -h localhost -p 10000
Fiscalia.Proxy=fiscalia/fiscalia:tcp -h localhost -p 10004
ElectoralDB.Proxy=electoraldb:tcp -h localhost -p 10003
ReliableMsg.Proxy=reliablemsg:tcp -h localhost -p 10004

# Configuración general de Ice
Ice.Default.Host=localhost
Ice.ThreadPool.Server.Size=4
Ice.ThreadPool.Server.SizeMax=8
Ice.ThreadPool.Client.Size=4
Ice.ThreadPool.Client.SizeMax=8

# Configuración de seguridad (opcional)
Ice.ACM.Client=0
Ice.ACM.Server=0

# Propiedades del servicio
Votacion.Name=mesavotacion
Votacion.Category=votacion 